import 'package:flutter/material.dart';

class DevicesListScreen extends StatelessWidget {
  const DevicesListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("DevicesListScreen");
  }
}